using System;
using System.Collections.Generic;
using System.Text;

namespace MyCRM.Shared.Constants
{
    public class Expressions<T>
    {
        //public static Func<T, bool, bool> CheckUserEmployeeIsSupervisorFindT
        //{
        //    get
        //    {
        //        Func<T,bool, bool> findPeoplesFunc;
        //        if (user.Employee.IsSupervisor)
        //        {
        //            findPeoplesFunc = (p) => p.Employee.OrganizationId == user.OrganizationId;
        //        }
        //        else
        //        {
        //            findPeoplesFunc = (p) => p.Employee.Id == user.Employee.Id;
        //        }
        //    }
        //}
    }
}